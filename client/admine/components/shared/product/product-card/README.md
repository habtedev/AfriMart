// This folder will contain components for displaying and managing individual product cards
// Example: ProductCard.tsx, ProductCardList.tsx, etc.
// Product card fields (from backend): title, image, price, offPrice, description, stock, rating, reviewCount, sku, category, isBestSeller, isTodayDeal, createdAt
